services:
  social_automation.commands:
    class: Drupal\social_automation\Commands\AutomationCommands
    arguments: ['@config.factory', '@config.storage', '@plugin.manager.automation_trigger']
    tags:
      - { name: drush.command }
